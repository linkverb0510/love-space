# 长期记忆

- 公开根入口与私密共享入口必须分流：根地址不连接 Supabase、不保存业务数据；私密入口使用 `?space=private`。
- Supabase 前端只能使用 publishable/anon key；真实安全边界是 Auth、RLS 和 private Storage，不能用前端 hash 代替认证。
- 共享数据禁止整空间快照回写，新增、更新、删除都必须按实体和 `version` 写入，并处理零行影响冲突。
- `Photo.timelineEntryId` 是照片与时间线的唯一关联来源；不要恢复 `timeline_entries.photo_ids` 应用层依赖。
- 空间日期固定为 `Asia/Hong_Kong`，跨设备日期计算必须从同一时区的日历日期派生。
- 图片元数据放 localStorage 或 Postgres，图片 Blob 放 IndexedDB 或 Supabase Storage，不能把 Base64 照片写进 localStorage。
- 修改完成后至少运行 `npm test`、`npm run build`，涉及 UI 时补桌面端和 390px Playwright 冒烟。
