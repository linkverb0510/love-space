# 开发记录 2026-08-03

## 需求

把原型从“浏览器各自保存、前端密码门禁、公开 Supabase demo”收敛为可共享的私密空间：公开根地址只展示空数据，私密地址通过 Supabase Auth 登录；实体写入需要版本保护；照片进入可靠的 Storage 生命周期；日期统一按香港时区计算；并保留 B1 相册页轴与移动端体验。

## 本次结果

- `SpaceData`、本地存储和实体仓储增加版本字段，移除 `saveSnapshot`，更新/删除使用旧版本条件，冲突会显示可见状态。
- 新增 `auth.ts`、`errors.ts`、运行入口判断和 Service Worker 缓存策略；共享密码改为 Supabase Auth `signInWithPassword`。
- 根地址强制使用空数据只读预览；`?space=private` 才创建 Supabase 客户端并进入登录流程。
- 本地 Vite 开发服务器的根入口改为可编辑本地空间，使用浏览器 localStorage/IndexedDB 做试验；生产构建根入口继续保持空数据只读预览。
- 新上传照片日期优先读取 EXIF `DateTimeOriginal`，其次识别文件名日期，再退回文件修改时间和业务备用日期；历史照片不伪造无法恢复的拍摄时间。
- 时间线新建回忆支持同时选择照片，回忆保存后先取得条目 ID，再将照片带 `timelineEntryId` 加入上传队列，因此时间线和照片墙共享同一条照片记录。
- 上传前校验单个媒体 20MB 上限；原文件保留原 MIME、字节数和尺寸，同时生成 720px 缩略图与 2048px 展示版。图片与同名 MOV/MP4 可配对为网页动态照片，本地多资产放入 IndexedDB 事务，Supabase 资产按需签发。
- 新增 `supabase/migrations/0002_private_space.sql`，回填后删除 `photo_ids`，关闭匿名表和 Storage 策略，增加版本与更新时间字段。
- 日期计算、首页今天、照片默认日期、计划完成日期统一使用 `Asia/Hong_Kong`。
- 弹层支持 Escape、初始焦点和焦点循环；同步状态显示加载中、保存中、已同步、冲突和失败。
- GitHub Pages 工作流改为接收 Supabase Secrets；缺少配置时私密入口明确报错，不静默回退。
- 照片墙按香港时区的月份和日期分组，桌面端使用连续纵向绳线与左右交错纸卡，移动端使用左侧绳线单列布局；墙面只加载缩略图/展示图，详情层提供原图下载和动态片段播放。

## 验证

`npm test -- --run`：14 个测试文件、54 个测试通过。`npm run build`：TypeScript 与 Vite 构建通过。浏览器已验证公开根入口为空且只读、私密入口缺配置时明确报错、390px 移动布局可用；照片墙和时间线回忆附件已完成桌面/移动布局及端到端烟测。

## 手动配置

需要在 Supabase 创建共享 Auth 账号、备份后依次执行四份迁移、把账号 UUID 加入 `private-space` 的 `space_members`，再在 GitHub Actions Secrets 配置 Supabase URL、anon/publishable key、私密空间路径和共享邮箱。明文密码和 `service_role` key 不进入代码或对话。历史照片在 `0003_photo_assets.sql` 后继续使用旧展示路径，不能恢复不存在的原图；`0004_roles.sql` 只增加作者标记，不改变权限模型。

## Supabase 线上配置完成记录

- Supabase 项目已创建并完成 `0001_initial.sql`、`0002_private_space.sql` 两份迁移。
- 已创建共享 Auth 账号 `LW_6459@163.com`，并绑定到 `private-space`；账号邮箱在 Supabase 内部显示为小写形式。
- 已将原公开演示空间中的“第一次牵手”迁移到私密空间；公开演示空间的回忆、照片和计划已清空。
- 当前私密空间状态：保留 1 条回忆，开始日期、照片和计划为空，后续由页面直接编辑。
- GitHub Actions 已配置 `VITE_SHARED_AUTH_EMAIL`、`VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`，并成功发布到 GitHub Pages。
- 线上私密入口：<https://linkverb0510.github.io/love-space/?space=private>
- 公开入口：<https://linkverb0510.github.io/love-space/>。该入口为空数据只读预览，不承载私密内容。
- 已验证私密入口登录成功，页面从 Supabase 读取到“第一次牵手”，同步状态显示为“已同步”。
- 初始密码只保存在 Supabase Auth 中，不写入代码、仓库或文档。修改密码时进入 Supabase Dashboard 的 Authentication -> Users，找到该账号后选择 `Reset password` 或发送密码重置邮件；修改后无需重新部署。
- 已删除旧版前端密码哈希 Secret，前端密码比较不再作为安全边界。

## 照片墙媒体改造记录

- `Photo` 兼容旧 `src/storagePath`，新增 `original`、`display`、`thumbnail`、`motion` 资产路径与媒体元数据。
- 上传并发限制为 2；任一远程资产或元数据写入失败会清理已上传对象，本地资产使用 IndexedDB 事务保存。
- HEIC 不新增转码依赖；浏览器无法解码时保留原文件并提示用户下载。动态照片不接入原生 LivePhotosKit，使用静态封面加 `<video controls playsInline>` 的可降级方案。

## 双角色视觉改造记录

- 新增统一角色语义：L、W、一起、未标记。角色表示内容添加者或计划负责人，不等同于 Supabase 登录权限。
- 首页加入 L/W 双角色区域；回忆、照片、计划和设置统一使用 `RoleBadge` 与 `RolePicker`，颜色始终和文字/字母同时呈现。
- 页面背景改为更浅的亚麻白；L 使用海蓝色，W 使用粉色，一起使用暖金色，危险操作改用独立深红色。
- 使用少量本地透明角色插画、双线纸卡边缘、角色夹子和票据式标签增强 Lolita 纸品感，真实照片仍是主要内容。
- 当前身份按空间路径保存在当前浏览器，默认 L；历史没有作者字段的内容加载为“未标记”，不自动推断归属。
- 新增 `supabase/migrations/0004_roles.sql`。迁移私密网站前必须完成数据备份并在本地验证；不修改 `space_members.role` 的权限约束。
- 浏览器烟测覆盖 390px 与 1440px 页面、设置中切换 W、回忆表单继承 W、Escape 关闭弹层、横向溢出检查和视觉截图。

## Atelier Lolita 视觉落地记录

- 新增 `src/components/LolitaDecor.tsx`：`LaceTrim` 用重复扇形路径和边缘圆点构成真实蕾丝，`RoseIllustration` 明确绘制花瓣、花心、叶片和花心旋线，`LolitaPaperDecor` 用于首页纸卡，`LolitaPageDecor` 用于页面背景。
- 首页、时间线和照片墙统一挂载背景装饰；页面装饰使用 `aria-hidden` 和 `pointer-events: none`，不会拦截照片、表单或导航操作。
- 新增的唯一持续装饰动效是缎带轻摆；全局 `prefers-reduced-motion: reduce` 会收敛动画和过渡。照片、时间线文字和操作按钮保持更高层级与更高对比度。
- 1440px 截图确认双层蕾丝、玫瑰和缎带可辨认；390px 截图确认装饰缩小、照片墙保持单列、底部导航和安全区不重叠，`scrollWidth` 未超过视口。
- 本次本地烟测使用用户提供的截图作为临时照片，照片日期按文件日期显示为 2026-08-03；测试数据只存在本地浏览器，迁移线上前应清除或使用独立浏览器配置。
